# Installed model-selector smoke

This is a read-only simulated routing test, not a business task. Use the installed plugin's orchestrating-multi-model-work Skill and its model-routing and handoff references. Report the exact installed version and files you read. Do not use the source repository, edit files, run a real phase, create child tasks, contact external services, or assume that a simulated platform was tested with a real client.

Scenario 1: A frozen contract, version selector-smoke-1, exists. Codex can select Astra, Terra, Sol, and Luna; their role capabilities and included cost are confirmed. There is no saved profile yet. Show the user's first selector and state whether work starts before the user responds. The actual run has not started, but the host supports task/model readback.

Scenario 2: The user already selected recommended-fit. Claude Code uses the plugin installed directly from GitHub; its manifest name is turning-ideas-into-projects. Claude Pro exposes Fable 5.1, Opus 5, and Sonnet 5, with verified role capabilities. Opus and Sonnet are within allowance; Fable consumes extra credits that have not been approved. Show the sibling namespace and requested role route. No child has been launched yet.

Scenario 3: WorkBuddy has a saved recommended-fit preference, but Kimi's included allowance is now exhausted. A replacement model is visible, but its cost is unknown. State the next action and whether automatic dispatch or credit purchase is permitted.

Scenario 4: A future model is visible with current official capability evidence, confirmed included cost, and a working host task/model readback mechanism, but has never run this project. The user explicitly selects it for planning. Distinguish preflight from post-launch actual-model evidence; do not block solely because there is no prior run.

End with a concise result for each scenario and the marker INSTALLED_SELECTOR_SMOKE_COMPLETE. These are simulated routing results, not real Claude, WorkBuddy, or future-model execution evidence.
